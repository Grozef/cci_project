{% extends 'base.html.twig' %}

{% block title %}Nouvelles informations{% endblock %}

{% block body %}

        {% for message in app.flashes('warning') %}
            <div class="alert alert-warning mt-4">
                {{ message }}
            </div>
        {% endfor %}
        {% for messages in app.flashes ('success') %}
        <div class="alert alert-dismissible alert-success ml-4 mt-4">
            {{ message}}
        </div>
        {% endfor %} 
        
    <h1>Créer de nouvelles informations</h1>

    {{ include('pages/user_info/_form.html.twig') }}

    <a href="{{ path('app_user_info_index') }}">Retour a la liste</a>
{% endblock %}
